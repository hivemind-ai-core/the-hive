Break the plan down into simple, single-purpose, atomic units of work and store each as a task in `tt` using MCP.
Make sure each task has a clear, detailed description, a clear definition of done, and has any relevant or necessary dependencies set (only set dependencies if one task needs to be done before another task).
